diff --git a/.github/workflows/release.yml b/.github/workflows/release.yml
index 194e3c41..5201fa91 100644
--- a/.github/workflows/release.yml
+++ b/.github/workflows/release.yml
@@ -14,6 +14,11 @@ permissions:
 
 jobs:
   release:
+    # Fork: releases run only on upstream getarcaneapp/arcane. This fork has no
+    # goreleaser-pro key / cosign / macOS notary / Depot, so the job is gated
+    # off here. Re-enable with a fork-owned, image-only release to Quay
+    # (quay.io/iguessipentest/arcane-podman). See patch/README.md.
+    if: github.repository == 'getarcaneapp/arcane'
     runs-on: depot-ubuntu-24.04-16
 
     env:
